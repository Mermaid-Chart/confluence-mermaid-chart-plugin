permissions:
  external:
    images:
      - https://www.mermaidchart.com/*
    fetch:
      backend:
        - www.mermaidchart.com
  scopes:
    - storage:app
modules:
  # confluence:contentAction:
  #   - key: epa-hello-world-content-action
  #     resource: main
  #     resolver:
  #       function: resolver
  #     render: native
  #     title: EPA
  macro:
    - key: mermaid
      title: Mermaid chart X
      description: Mermaid charts
      resource: main
      resolver:
        function: resolver
      render: native
      config: true

  confluence:globalSettings:
    - key: mchart-settings
      function: main-settings
      title: Mermaid Chart Settings (UI Kit 2)

  function:
    - key: resolver
      handler: index.handler
    # - key: config-function-key
    #   handler: index.config

    #   # - key: main
    #   #   handler: index.run
    - key: main-settings
      handler: settings.run

resources:
  - key: main
    path: src/frontend/index.jsx

app:
  id: ari:cloud:ecosystem::app/a36ca994-14b4-48d0-8970-e4515eae559f
  name: mc-ui-kit2
